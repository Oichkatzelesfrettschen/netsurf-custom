# Host development profile.
#
# This profile intentionally keeps the existing developer-oriented
# configuration path. Local Makefile.config overrides still apply before
# this file; command-line make variables remain the strongest override.
